# Employee Turnover Chi-square Test


This repository runs a chi-square test to check whether leaving the company is associated with gender.


## Contents
- `chi_square.py` — main script
- `sample_data.csv` — example dataset
- `requirements.txt` — required packages


## Usage
1. Create a virtual environment (recommended):


```bash
python -m venv venv
source venv/bin/activate # macOS / Linux
venv\Scripts\activate # Windows
